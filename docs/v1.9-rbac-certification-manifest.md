# Nexus v1.9 RBAC Certification Manifest

## Certification status

Status: **Certified after PR validation and merge**

Certified baseline commit on `main`:

`8fd217d2e1cb983dd88c334c89b2500d4caf1e3c`

Merged PR:

- PR #2 — `[codex] Add v1.9 Admin API role-based authorization`

## Scope certified

v1.9 introduced a narrow role-based authorization layer for the existing read-only Nexus Admin API.

Certified changes:

- Centralized RBAC at the Admin API HTTP boundary.
- Existing `X-Nexus-Admin-Key` authentication retained.
- Existing `AdminKey` remains backward-compatible and grants Admin access.
- Optional `AuditorKey` and `ViewerKey` configuration entries added.
- `/health` remains open.
- `/trust/{module}` requires Viewer or higher.
- `/dqs` requires Auditor or higher.
- `/audit` requires Auditor or higher.
- Unknown protected endpoints default to Admin.
- `401 unauthorized` for missing or invalid keys.
- `403 forbidden` for valid keys with insufficient role.

## Validation evidence

PR head validated before merge:

`b2a49997100b40a1d5c1b4fb42ca177a407a073a`

GitHub Actions results on PR head:

- Nexus CI: **PASS**
- Nexus Simulation (Strict Gate): **PASS**

Post-merge note:

- No separate post-merge workflow run was attached to the squash merge commit at the time of certification check.
- The merge was performed only after both required PR workflows passed on the exact PR head SHA.

## Governance boundaries

This certification does **not** include:

- JWT authentication.
- User database or identity provider integration.
- Dashboard work.
- Write-side Admin API behavior.
- Broader Nexus policy engine changes.
- Simulation behavior changes.
- Persistence schema expansion.

## Next development boundary

Future v1.9+ work should continue in separate, narrow PRs.

Recommended next slices:

1. Add Admin API authorization tests.
2. Add deployment notes for setting role keys through environment variables.
3. Plan future JWT or external identity provider integration separately.
